<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="WelcomeScreen_Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Secondary Item. -->
        <item name="colorPrimary">@color/Color_10</item>
        <item name="android:windowBackground">@color/Color_10</item>
        <item name="android:orientation">vertical</item>
        <!-- Status bar & Nav Bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Color_10</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="LoginScreen_Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Secondary Item. -->
        <item name="colorPrimary">@color/Color_1</item>
        <item name="android:windowBackground">@color/Color_1</item>
        <item name="android:orientation">vertical</item>
        <!-- Status bar & Nav Bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Color_1</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="DashboardScreen_Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Secondary Item. -->
        <item name="colorPrimary">@color/Color_1</item>
        <item name="android:windowBackground">@color/Color_11</item>
        <item name="android:orientation">vertical</item>
        <!-- Status bar & Nav Bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Color_1</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="PresensiScreen_Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Secondary Item. -->
        <item name="colorPrimary">@color/Color_1</item>
        <item name="android:windowBackground">@color/Color_1</item>
        <item name="android:orientation">vertical</item>
        <!-- Status bar & Nav Bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Color_1</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Profile_Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Secondary Item. -->
        <item name="colorPrimary">@color/Color_1</item>
        <item name="android:windowBackground">@color/Color_1</item>
        <item name="android:orientation">vertical</item>
        <!-- Status bar & Nav Bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Color_1</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="RekapAbsensi_Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Secondary Item. -->
        <item name="colorPrimary">@color/Color_1</item>
        <item name="android:windowBackground">@color/Color_11</item>
        <item name="android:orientation">vertical</item>
        <!-- Status bar & Nav Bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Color_1</item>
        <!-- Customize your theme here. -->
    </style>

</resources>